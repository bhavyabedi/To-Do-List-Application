# To Do List Application

This application enables users to store, edit, delete, and categorize their tasks in one convenient place. It leverages local storage using SQLite to maintain persistent data. Tasks are displayed in a scrollable list at the top of the home screen and can be sorted according to user preferences.
